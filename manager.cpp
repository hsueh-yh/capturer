#include "manager.h"
#include "mtndn-utils.h"

std::string
Manager::addLocalStream(const MediaStreamParams &params,
                        IExternalCapturer ** const capturer)
{
    std::string streamPrefix = "";

    return streamPrefix;
}
